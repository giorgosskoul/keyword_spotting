## Keyphrase spotting 

# Important files:

data/download_datasets.ipynb : download the datasets
feature_extraction.py : extract the audio embeddings from the data
validation.py : train and validate models on extracted embeddings